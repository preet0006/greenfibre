import LoginClient from "./LoginClient";

export const metadata = {
  title: "Sign In",
  description:
    "Sign in to your Green Fibre account to track orders and manage your profile.",
};

export default function LoginPage() {
  return <LoginClient />;
}
